terraform {
  backend "s3" {
    bucket       = "replace-with-your-terraform-state-bucket"
    key          = "failure-resolver/poc/terraform.tfstate"
    region       = "us-east-1"
    use_lockfile = true
    encrypt      = true
  }
}
